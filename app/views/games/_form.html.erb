<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :played_at %><br>
    <%= f.date_select :played_at %>
  </div>
  <div class="field">
    <%= f.label :opponent %><br>
    <%= f.select :opponent, options_for_select(User.all.map{|s|[s.email, s.id]}) %>
  </div>
  <div class="field">
    <%= f.label :your_score %><br>
    <%= f.number_field :your_score %>
  </div>
  <div class="field">
    <%= f.label :their_score %><br>
    <%= f.number_field :their_score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
